services:
  backend:
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    command: ["python", "manage.py", "runserver", "0.0.0.0:8000"]
  webapp:
    volumes:
      - ./webapp:/app
    ports:
      - "5173:5173"
    command: ["npm", "run", "docker:dev"]
